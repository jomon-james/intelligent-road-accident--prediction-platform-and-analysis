-- This file can be used to initialize the database schema
-- However, SQLAlchemy will create the schema automatically

-- Optional: Additional indexes for performance
-- CREATE INDEX idx_accidents_severity_date ON accidents(severity, accident_date);
-- CREATE INDEX idx_accidents_location ON accidents(latitude, longitude);
-- CREATE INDEX idx_predictions_created ON predictions(created_at);